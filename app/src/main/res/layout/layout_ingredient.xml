<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/dash_text_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:text="@string/long_dash"
        android:textColor="@color/colorText"
        android:textSize="@dimen/text_size_large"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/ingredient_text_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:layout_marginStart="6dp"
        android:textColor="@color/colorText"
        android:textSize="@dimen/text_size_large"
        app:layout_constraintEnd_toStartOf="@id/remove_image_view"
        app:layout_constraintStart_toEndOf="@id/dash_text_view"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/remove_image_view"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:background="@drawable/fab_background"
        android:elevation="1dp"
        android:src="@drawable/ic_minus"
        app:layout_constraintBottom_toBottomOf="@id/ingredient_text_view"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/ingredient_text_view" />
</androidx.constraintlayout.widget.ConstraintLayout>